Backend note: If you want level-up toasts to work, update TaskViewSet.complete() to return xp_earned and level_up in the response — the frontend is already written for it, the backend just doesn't send it yet.



One additional note — the backend AchievementSerializer also returns icon (an emoji string like '🏆'). Right now you're hardcoding <Trophy className="h-7 w-7" />. You could use the icon from the API:
typescript// Optional improvement — use server-side icon
<div className={cn('mb-3 grid h-14 w-14 place-items-center rounded-xl border text-2xl', ...)}>
  {a.icon || <Trophy className="h-7 w-7" />}
</div>



Backend note: To support incoming friend requests (people who sent YOU a request), your get_queryset needs to also return entries where friend=request.user AND status='pending'. Right now it only returns filter(user=request.user).


Backend fix needed: update_mission_progress should accept mission_id and update that specific mission, not all missions of a type. Currently there's no per-mission progress update endpoint.


7. UserProfileView at /api/profile/ conflicts with progress app
UserProfileView (in users/views.py) serves GET/PATCH /api/auth/profile/ via UserUpdateSerializer — this is the user profile (bio, avatar, etc.). But the progress app has UserProgressProfileViewSet at /api/profile/ — this is the gamification profile. These likely conflict in your URL router or serve completely different data from the same-looking path. You need to reconcile this.



